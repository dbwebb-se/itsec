{
  "name": "itsec-bank-app",
  "version": "1.0.0",
  "repository": "https://github.com/dbwebb-se/itsec-proj",
  "description": "Bank application for project in itsec course.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "linter": "eslint -c .eslintrc.json --ext=mjs,js .",
    "linterFix": "eslint -c .eslintrc.json --ext=mjs,js . -- --fix",
    "clean": "rm -rf build/",
    "clean-all": "npm run clean && rm -rf node_modules/ && rm -f package-lock.json"
  },
  "author": "Marie Grahn",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.9",
    "eslint": "^8.36.0",
    "express": "^4.17.1",
    "express-session": "^1.16.2",
    "sqlite3": "^5.1.6"
  }
}
